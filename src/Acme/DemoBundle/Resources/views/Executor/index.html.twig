{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Executor list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Firstname</th>
                <th>Middlename</th>
                <th>Lastname</th>
                <th>Birthday</th>
                <th>Careerbeginningdate</th>
                <th>Phone</th>
                <th>Email</th>
                <th>Skype</th>
                <th>Technologiesusing</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('executor_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.firstName }}</td>
                <td>{{ entity.middleName }}</td>
                <td>{{ entity.lastName }}</td>
                <td>{% if entity.birthday %}{{ entity.birthday|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.careerBeginningDate %}{{ entity.careerBeginningDate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.phone }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.skype }}</td>
                <td>{{ entity.technologiesUsing }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('executor_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('executor_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('executor_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
